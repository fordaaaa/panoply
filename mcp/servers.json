{
  "$comment": "Canonical MCP roster. build.mjs renders this into .mcp.json (Claude Code), the mcp block of opencode.json, and .cursor/mcp.json. Edit here only. profile 'default' ships in every generated config; 'opt-in' ships only with `node build.mjs --with <name>`.",

  "github": {
    "profile": "default",
    "transport": "stdio",
    "command": "node",
    "args": ["servers/gh/index.mjs"],
    "auth": "token (GITHUB_MCP_TOKEN env var)",
    "why": "/cr-run files issues and /cr-fix opens PRs. This server's OAuth does not support dynamic client registration, so the in-agent OAuth flow fails. Our panoply-gh bridge forwards to api.githubcopilot.com with GITHUB_MCP_TOKEN as Bearer and filters toolsets to issues,pull_requests,repos (44 tools -> 38)."
  },

  "context7": {
    "profile": "default",
    "transport": "http",
    "url": "https://mcp.context7.com/mcp",
    "auth": "none",
    "why": "Version-accurate library docs. Cuts hallucinated APIs in /cr-fix on unfamiliar dependencies. Two tools, ~400 tokens, no auth - the cheapest server here."
  },

  "playwright": {
    "profile": "default",
    "transport": "stdio",
    "command": "node",
    "args": ["servers/browser/index.mjs"],
    "auth": "none",
    "why": "Drives a real browser so /verify can check a web UI actually renders. Our panoply-browser wrapper pins the profile under ~/.panoply/browser. ~21 tools (~5k tokens); worth it on repos with a front end."
  },

  "showcase": {
    "profile": "opt-in",
    "transport": "stdio",
    "command": "node",
    "args": ["servers/showcase/index.mjs"],
    "auth": "none",
    "why": "Records screen captures of an agent session with ffmpeg and exports videos to a user-accessible folder. 5 tools, near-zero tokens; opt in when you want session recordings."
  }
}
