# Per-repo answers written by the bootstrap step (filing/tracker/autoclose).
# Consumers of these commands may commit their own; we don't publish ours.
.ai-skills/

# Local editor/agent state
.claude/settings.local.json

node_modules/
