# Per-repo answers written by the bootstrap step (filing/autoclose), plus the
# artifacts the commands write. Consumers may commit their own map/specs;
# we don't publish ours.
.panoply/

# Local editor/agent state
.claude/settings.local.json

node_modules/
__pycache__/
*.pyc
